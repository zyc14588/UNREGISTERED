# OpenCode Instructions — TRPG Sandbox Campaign

> 可按需复制/合并到项目根目录 `AGENTS.md`。如果项目已有 AGENTS.md，请合并而不是覆盖。

## Campaign Operating Rules

1. 对任何战役 canon 任务，先读 `campaign/00-campaign.md` 和 `campaign/state/world-state.yaml`。
2. 不替玩家角色决定行动、台词、感受或成长结论。
3. 设计 pressures / situations / actors / clocks / consequences，不设计玩家必须依次执行的 plot beats。
4. 所有 active situation、重要 NPC 目标、派系目标和地点压力都要有具体 `If Ignored`。
5. 脑暴默认是 `PROPOSAL`；只有已发生或 GM 明确确认才是 `CANON`。
6. 临场采用的新事实记为 provisional canon，赛后由 `campaign-state-keeper` 转正。
7. GM-facing 内容要短、可扫描、可操作；player-facing read-aloud / handout 才使用文学性。
8. 自制规则设计调用 `trpg-system-designer`；战役内容需要引用正式数值时调用 `rules-system-adapter`；不要默认 D&D。
9. 规则草案使用 `IDEA/PROPOSAL/PROTOTYPE/PLAYTEST/CANON/DEPRECATED` 生命周期；只有 `CANON` 或明确测试中的规则能进入对应内容。
10. 每次规则测试记录到 `campaign/rules/playtest-log.md`；一次只改少量根因参数，并记录回归风险。
11. 每次 session 后更新 recap、world state、timeline 和被触及实体的活动记录。
12. 玩家绕开准备内容时，让世界继续变化，不强行把旧内容重新摆到必经路线上。
